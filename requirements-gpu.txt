# Optional RAPIDS GPU accelerators for the NVIDIA GB10 box (aarch64, CUDA 12 wheels;
# driver 580 runs cu12 fine). NOT installed by CI or the dev/demo venv — the code
# falls back to CPU (networkx / Polars-CPU / pandas) when these are absent, so the
# live demo is never blocked. Install ON THE BOX to activate the GPU paths:
#
#   pip install --extra-index-url=https://pypi.nvidia.com -r requirements-gpu.txt
#
# Then enable per path (off by default):
#   URBANOS_GPU_GRAPH=1   → nx-cugraph backend for the substrate shortest-paths bake
#   URBANOS_GPU_DF=1      → cuDF GPU engine behind Polars' collect(engine="gpu")
# Verify with:  make gpu-check
--extra-index-url https://pypi.nvidia.com

# GPU backend for networkx (substrate dijkstra bake — src/urbanos/kernel/kernel/state.py).
nx-cugraph-cu12

# RAPIDS cuDF engine behind Polars' GPU engine (civic ingest — src/urbanos/risk/ingest/loader.py).
cudf-polars-cu12

# cuOpt LP solver for the optimal evacuation max-flow (URBANOS_GPU_FLOW=1 — src/urbanos/kernel/flow.py).
# NOTE: this is for the FLOW LP, not the lever optimizer — cuOpt cannot evaluate the
# black-box simulation-in-the-loop search (optimize.py stays grid-search; ADR-0025).
cuopt-cu12

# cuML GPU KMeans for spatial risk-hotspot clustering (URBANOS_GPU_CLUSTER=1 — src/urbanos/risk/cluster.py).
cuml-cu12

# --- Inference + physics-ML seams (ADR-0027), box-side, opt-in, CPU/exact fallback ---
#
# TensorRT-LLM — serve the Nemotron narrator behind `trtllm-serve` (OpenAI-compatible),
# then set LLM_RUNTIME=tensorrt-llm + point LLM_BASE_URL at it. The narrator client is
# unchanged (it only speaks OpenAI HTTP). NB: do NOT pip-install bare-metal on aarch64 —
# it hits a torch-C++-ABI wall (ADR-0027 §Box verification). Use the NGC container
# (nvcr.io/nvidia/tensorrt-llm/release). Box-proven as a CAPABILITY, not a speedup
# (single-stream decode is NOT faster than Ollama: 54.5 vs 61.2 tok/s).
# tensorrt-llm  (use the NGC container instead — see ADR-0027)
#
# PhysicsNeMo (formerly Modulus) — trained surrogate of the optimizer objective J(levers)
# for city-scale search (URBANOS_SURROGATE=1 + URBANOS_SURROGATE_CKPT=<path> —
# src/urbanos/kernel/surrogate.py). SHIPPED AS AN INTERFACE ONLY: no trained checkpoint, so the
# exact kernel remains the reference and decides every result; training is the next step.
# nvidia-physicsnemo
